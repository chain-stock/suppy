[pytest]
addopts = --cov=simulation --cov-report=xml

[tox]
envlist = py39
skipsdist = True

[testenv]
deps =
    poetry

commands_pre =
    poetry run pip install -U pip
    poetry install -v

commands =
    poetry run pytest test -n auto {posargs}
